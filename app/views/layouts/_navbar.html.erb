<%#= link_to "Log out", destroy_user_session_path, :method => "delete" if current_user %>
<div class="container navicons">
  <div class="row">
    <div class="navbar2">
      <div class="col-sm-4 col-md-4 col-xs-4 centered navbar1">
  	    <h5>
  	      <%= link_to "<", :back, :class => "btn" %>
  	    </h5>
      </div>
      <div class="col-sm-4 col-md-4 col-xs-4 centered navbar1">
      	<h5>
          <% if controller_name == "users" %>
            <%= link_to "", root_path, :class => "btn glyphicon glyphicon-plus", :id => "nav_host_event_link" %>
          <% else %>
      		<%= link_to "", new_event_path(date: date, place_id: place_id, distance: distance), :class => "btn glyphicon glyphicon-plus", :id => "nav_host_event_link" %>
          <% end %>
      	</h5>
      </div>
      <div class="col-sm-4 col-md-4 col-xs-4 centered navbar1">
      	<h5>
          <% if current_user %>
      		  <%= link_to "", user_path(current_user), :class => "btn glyphicon glyphicon-calendar" %>
          <% else %>
            <%= link_to "", root_path, :class => "btn glyphicon glyphicon-calendar" %>
          <% end %>
      	</h5>
      </div>
    </div>
  </div>
</div>

