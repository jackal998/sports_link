<%#= link_to "Log out", destroy_user_session_path, :method => "delete" if current_user %>
<div class="navicons">
  <div class="row">
    <div class="navbar2">
      <div class="col-sm-4 col-md-4 col-xs-4 centered navbar1">
  	    <h5 style="padding-top: 5px;">
  	      <%= link_to root_path do %>
            <span class="glyphicon glyphicon-home" aria-hidden="true"></span>
          <% end %>
  	    </h5>
      </div>
      <div class="col-sm-4 col-md-4 col-xs-4 centered navbar1">
      	<h5>
          <% if controller_name == "users" %>
            <%= link_to image_tag("host.png", height: '25', width: 'auto'), root_path, :id => "nav_host_event_link" %>
          <% else %>
      		  <%= link_to image_tag("host.png", height: '25', width: 'auto'), new_event_path(date: date, place_id: place_id, distance: distance), :id => "nav_host_event_link" %>
          <% end %>
      	</h5>
      </div>
      <div class="col-sm-4 col-md-4 col-xs-4 centered navbar1">
      	<h5>
          <% if current_user %>
      		  <%= link_to image_tag("calendar.png"), user_path(current_user) %>
          <% else %>
            <%= link_to image_tag("calendar.png"), new_user_session_path %>
          <% end %>
      	</h5>
      </div>
    </div>
  </div>
</div>